<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-amber-50 mb-5 text-black-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Administrātori</h1>
    <%= link_to 'Jauns', new_user_path, class: "rounded-lg py-3 px-5 bg-amber-300 text-black block font-medium" %>
  </div>

  <div id="users" class="min-w-full">
    <%= render @users %>
  </div>
</div>
